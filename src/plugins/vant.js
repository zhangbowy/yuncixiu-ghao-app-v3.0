import Vue from 'vue'
import {
  Swipe,
  SwipeItem,
  Lazyload,
  Search,
  NoticeBar,
  NavBar,
  Tab,
  Tabs,
  Form,
  Field,
  Button,
  Checkbox,
  Grid,
  GridItem,
  ActionSheet,
  TreeSelect, GoodsAction, GoodsActionIcon, GoodsActionButton,
  AddressList, AddressEdit,
  ShareSheet,
  Area,
  SubmitBar,
  Col, Row
} from 'vant'

Vue.use(
  Swipe
).use(
  SwipeItem
).use(
  Lazyload
).use(
  Search
).use(
  NoticeBar
).use(
  NavBar
).use(
  Tab
).use(
  Tabs
).use(
  NavBar
).use(
  Form
).use(
  Field
).use(
  Button
).use(
  Checkbox
).use(
  Grid
).use(
  GridItem
).use(
  ActionSheet
).use(
  TreeSelect
).use(
  GoodsAction
).use(
  GoodsActionIcon
).use(
  GoodsActionButton
).use(
  AddressList
).use(
  AddressEdit
).use(
  ShareSheet
).use(
  Area
).use(
  SubmitBar
).use(Col).use(Row)
