import Vue from 'vue'
import {
  Swipe,
  SwipeItem,
  Lazyload,
  Search,
  NoticeBar,
  NavBar,
  Tab,
  Tabs,
  Form,
  Field,
  Button,
  Checkbox,
  Grid,
  GridItem,
  ActionSheet
} from 'vant'

Vue.use(
  Swipe
).use(
  SwipeItem
).use(
  Lazyload
).use(
  Search
).use(
  NoticeBar
).use(
  NavBar
).use(
  Tab
).use(
  Tabs
).use(
  NavBar
).use(
  Form
).use(
  Field
).use(
  Button
).use(
  Checkbox
).use(
  Grid
).use(
  GridItem
).use(
  ActionSheet
)
